#include "color.h"


/*
struct Color
{
	Real r,g,b;	
	
	Color() {}
	
	Color(Real rr, Real gg, Real bb) { 
		r = rr; 
		g = gg; 
		b = bb; 
		
		assert(0.0 <= rr && rr <= 1.0);
		assert(0.0 <= gg && gg <= 1.0);
		assert(0.0 <= bb && bb <= 1.0);
	}
	
};

Color white(1,1,1);
Color red(1,0,0);
Color green(0,1,0);
Color blue(0,0,1);
Color yellow(1,1,0);
Color cyan(0,1,1);
Color magenta(1,0,1);
Color black(0,0,0);


void mov(Color &a, Real r, Real g, Real b) {
	a.r = r; a.g = g; a.b = b;
}


void mov(Color &a, Color& b) {
	mov(a, b.r, b.g, b.b);
}

void scale(Color &r, Color &c, Color &s) {
	r.r = c.r * s.r;
	r.g = c.g * s.g;
	r.b = c.b * s.b;
}


void mul(Color &res, Color& c, Real f) {
	res.r = c.r*f;
	res.g = c.g*f;
	res.b = c.b*f;
}

*/
